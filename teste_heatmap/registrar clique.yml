info:
  name: registrar clique
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/sessions/{{sessionId}}/clicks
  body:
    type: json
    data: |-
      {
        "x": 195.5,
        "y": 422.0,
        "intensity": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
