info:
  name: deletar click
  type: http
  seq: 8

http:
  method: DELETE
  url: http://localhost:3000/sessions/{{sessionId}}/clicks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
