info:
  name: criando sessao
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/sessions
  body:
    type: json
    data: |-
      {
        "name": "Criando Algo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
